{
  "name": "crawl-task",
  "version": "0.0.1",
  "description": "网页抓取，一个使用 Promise 和 JacksonTian/eventproxy 处理异步协作的例子",
  "main": "app.js",
  "scripts": {
    "start": "node app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "crawl",
    "task"
  ],
  "author": "hugojing",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^0.20.0",
    "eventproxy": "^0.3.4",
    "mongoose": "^4.4.8",
    "request": "^2.69.0"
  }
}
